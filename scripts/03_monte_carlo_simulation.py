# 1. Import necessary libraries
# 2. Define a function `run_monte_carlo_simulation(n_simulations, rows, balls)`
#    - Run multiple simulations of the Galton Board
#    - Compute and store key statistics (mean, variance, skewness)
#    - Save results in `outputs/monte_carlo_results.json`
# 3. If run as a script, execute `run_monte_carlo_simulation`